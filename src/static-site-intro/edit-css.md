# cssの編集
`.`をつけることでhtmlのclassを指定することができます。

```css
.main { 
    color: blue;
    font-style: italic;
}
```

このようにするとサイトのほうでは`Hello World`の色が変わり斜体になっているのがわかると思います。

cssのプロパティはたくさんあるので調べながらコツコツ覚えていきましょう。